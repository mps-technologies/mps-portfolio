[build]
  command = "npm run build"
  publish = "dist"

# Optional: set a specific Node version for Netlify's build image
[context.production.environment]
  NODE_VERSION = "18"

# Optional headers or redirects can be added below
#[[redirects]]
#  from = "/*"
#  to = "/index.html"
#  status = 200
